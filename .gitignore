# Build output
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
artifacts/
*.user
*.suo
.vs/
.vscode/
.idea/

# Python tooling used to regenerate artwork
__pycache__/
*.py[cod]
.pytest_cache/

# NuGet
*.nupkg
packages/

# DeskNote local data — never commit user notes
*.db
*.db-wal
*.db-shm
*.journal
local/

# Local AI models — never bundled in the repo (report p12: LLM을 앱 본체에 번들링하지 않는다)
# Anchored to the repo root: an unanchored `models/` also swallows src/DeskNote.Core/Models/.
/models/
*.gguf
*.onnx

# OS
Thumbs.db
desktop.ini

# Publish output (the built app is not committed)
dist/

# Test reports and coverage output
TestResults/
*.trx
.coverage
coverage/
