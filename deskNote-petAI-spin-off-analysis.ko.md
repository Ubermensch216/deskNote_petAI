# deskNote × pet_AI_v2 스핀오프 분석 보고서

분석일: 2026-08-31  
대상: [`Ubermensch216/deskNote`](https://github.com/Ubermensch216/deskNote), [`Ubermensch216/pet_AI_v2`](https://github.com/Ubermensch216/pet_AI_v2)  
분석 기준: 각 저장소 `main` 브랜치 최신 커밋 (`deskNote` `fedebb0e`, `pet_AI_v2` `a0f6ecbf`)

> 이 보고서는 GitHub의 실제 파일 트리, 설계 문서, 핵심 구현, 테스트 소스, 최근 커밋 이력을 읽은 정적 분석이다. 비공개 저장소를 로컬에서 빌드하거나 테스트를 실행한 결과는 아니며, 테스트 수는 테스트 소스 정의를 정적으로 집계한 값이다.

> **2026-09-03 기준 안내.** 이 문서는 스핀오프를 시작하기 전의 **분석 기록**이며 그대로 보존한다.
> 여기서 내린 결론은 [구현 계획서](deskNote-petAI-implementation-plan.ko.md)로 이어졌고, 그 계획과
> 실제 구현이 달라진 지점은 계획서의 `0. 진행 상황`에 정리돼 있다. 현재 동작하는 제품의 설명은
> [README.md](README.md)와 `docs/` 를 본다.

## 1. 결론

스핀오프의 가장 좋은 방향은 **deskNote에 펫 게임을 붙이는 것**이 아니라, **메모를 통해 사용자의 맥락을 기억하고 다시 꺼내 주는 ‘앰비언트 메모리 컴패니언’**을 만드는 것이다.

권장 제품 문장:

> 떠오른 것은 메모가 붙잡고, 잊을 때가 되면 반려 AI가 조용히 되돌려준다.

기술적으로는 `deskNote`를 기반 저장소로 삼고, `pet_AI_v2`의 Python/PySide6 코드를 함께 탑재하지 말고 핵심 도메인 아이디어를 C#으로 포팅하는 편이 맞다. 두 런타임과 두 UI 프레임워크, 두 SQLite 수명주기, 두 트레이·싱글 인스턴스 체계를 한 프로세스에 섞으면 제품 가치와 무관한 복잡성이 급증한다.

핵심 판단은 다음과 같다.

1. **기반은 deskNote**: 저장 안정성, 버전 이력, 하이브리드 검색, 로컬 AI 격리, Windows 네이티브 UI가 스핀오프의 뼈대다.
2. **pet_AI_v2에서는 관계 루프를 가져온다**: 순수 도메인 엔진, 선제 대화 빈도 제한, 결정적 성격·취향, 사용자 승인형 액션 카드, 장기 기억과 일일 리듬이 핵심 자산이다.
3. **성장과 돌봄의 의미를 바꾼다**: 밥·청결 게이지 대신 `기록 → 다시 보기 → 정리/완료`를 하루의 세 가지 관계 행동으로 삼는다. 메모를 안 썼다고 펫이 아프거나 관계가 감소하면 죄책감을 유발하는 생산성 게임이 된다.

   > **2026-09-01 개정**: 이 판단은 뒤집혔다. 베타 구현 결과 하루 최대 성장이 29점(알림 처리 미배선을 감안하면 실제 23점)에 그쳐, 펫이 키우는 대상이 아니라 앱 사용 영수증이 됐다. 성장 규칙 V2는 하루 100점을 앱 30점 · 돌봄 70점으로 나누고 포만감·청결도·기분 게이지를 도입한다. 다만 이 판단이 경계한 죄책감 유발은 그대로 막는다. 결석해도 경험치 차감·단계 하락·펫 소멸이 없고, 내려가는 것은 상태 게이지뿐이며, 돌봄 달성일은 연속일 필요가 없다. 현행 규칙은 [docs/companion.md](docs/companion.md)를 따른다.
4. **MVP에서는 사진 앨범·90개 업적·7종 수집·보상 상점을 제외한다**: 이것들은 검증된 메모리 컴패니언 루프 이후의 확장 기능이다.

## 2. 저장소 현황

| 항목 | deskNote | pet_AI_v2 |
|---|---:|---:|
| 주 기술 | C# / .NET 10 / WinUI 3 | Python 3.11+ / PySide6 |
| 기본 브랜치 | `main` | `main` |
| 소스 트리 파일 | 187 | 355 |
| 실질 코드 파일 | C# 135, XAML 10 | Python 93 |
| 테스트 파일 | 40개 C# 테스트 소스 | 22개 Python 테스트 소스 |
| 정적 집계 테스트 정의 | 356 | 716 |
| 저장소 표시 크기 | 약 1.0MB | 약 1.5MB |
| 실제 트리 바이트 | 약 2.1MB | 약 10.4MB |
| 최근 커밋 수 | 13 | 12 |
| CI 워크플로 | 없음 | 없음 |
| LICENSE | 없음 | 없음 |

`pet_AI_v2`의 실제 트리가 큰 이유는 `.codex-pytest-animation-20260824/` 아래 테스트 DB·JPG·PNG·GIF 252개가 커밋돼 있기 때문이다. `.gitignore`에는 이 경로 패턴이 없다. 이 산출물은 즉시 추적 중단 대상이며, 실제 사용자 사진인지 생성 픽스처인지 확인 후 민감 데이터 가능성이 있으면 Git 이력 정리까지 검토해야 한다.

두 저장소 모두 불과 며칠 동안 큰 기능량이 들어간 매우 젊은 코드베이스다. 테스트 수와 문서는 인상적이지만, 운영 안정성이나 장기 유지보수성은 아직 시간으로 검증되지 않았다. 커밋 메시지 대부분이 `ㄴ`, `s`, `d`, `정비`처럼 변경 의도를 설명하지 않아 회귀 추적과 릴리스 노트 자동화가 어렵다.

## 3. deskNote 분석

### 3.1 제품과 사용자 경험

`deskNote`는 **메모 → 기억 → AI → 자동화** 순서를 일관되게 지킨다. AI가 꺼져도 메모·검색·알림·이력은 동작하고, 로컬 모델이 없어도 앱 시작 경로가 지연되지 않는다. 스티키 노트의 핵심 마찰을 줄이는 데 집중했고, AI 결과는 미리보기 후 명시적으로 적용해야 원문이 바뀐다.

강한 제품 결정:

- 제목 필드 없이 본문 첫 줄에서 제목을 파생한다.
- Markdown 본문을 유일한 원본으로 두고 체크리스트·태그 테이블은 조회용 투영으로 다시 만든다.
- 메모는 300ms 디바운스 자동 저장하며, 포커스 상실·창 닫기·종료 시 즉시 flush한다.
- AI 일괄 변경 전에는 항상 원본 버전을 남기고, 복원 자체도 새 이력이 된다.
- 검색은 빠른 FTS 결과를 먼저 그리고, 의미 검색이 도착하면 RRF로 순위를 개선한다.
- Q&A는 사용한 메모를 근거로 보여 주며, 현재 메모로 범위를 지정하면 검색 결과로 범위를 넓히지 않는다.

### 3.2 아키텍처

의존 방향은 문서대로 명확하다.

```text
DeskNote.App ──> DeskNote.Core <── DeskNote.Data
      │                ▲
      └──> DeskNote.Ai ┘
```

- `DeskNote.Core`: UI·DB에 독립적인 모델, 계약, 정책
- `DeskNote.Data`: SQLite, FTS5, 마이그레이션, 리포지토리
- `DeskNote.Ai`: Ollama HTTP 구현, 프롬프트·응답 파싱
- `DeskNote.App`: WinUI 3 조립, 창·트레이·핫키·서비스

`App.xaml.cs`의 시작 순서도 좋다. DB 마이그레이션 → 언어 → 리포지토리 → 저장 파이프라인 → 크래시 복구 → 창 복원 → 알림·핫키·트레이 → AI probe 순서로, 메모가 AI를 기다리지 않는다.

저장 경로의 핵심은 [`NoteSavePipeline.cs`](https://github.com/Ubermensch216/deskNote/blob/main/src/DeskNote.App/Services/NoteSavePipeline.cs)와 [`AutosaveScheduler.cs`](https://github.com/Ubermensch216/deskNote/blob/main/src/DeskNote.Core/Services/AutosaveScheduler.cs)다. 저널을 먼저 기록하고 DB 저장 성공 후 지우므로, 남은 저널은 복구 후보가 된다. 본문 업데이트와 체크리스트·태그 투영은 하나의 트랜잭션에서 처리된다.

검색은 [`HybridLibrarySearch.cs`](https://github.com/Ubermensch216/deskNote/blob/main/src/DeskNote.App/Services/HybridLibrarySearch.cs), [`HybridRetriever.cs`](https://github.com/Ubermensch216/deskNote/blob/main/src/DeskNote.App/Services/HybridRetriever.cs), [`RankFusion.cs`](https://github.com/Ubermensch216/deskNote/blob/main/src/DeskNote.Core/Services/RankFusion.cs)로 분리돼 있다. 서로 비교할 수 없는 BM25와 코사인 거리를 점수 정규화하지 않고 순위 기반 RRF(`K=60`)로 합치는 선택이 안정적이다.

### 3.3 AI 안전성과 프라이버시

강점:

- 노트 본문을 시스템 지시가 아니라 명시적인 비신뢰 컨텍스트로 취급한다.
- 사용자가 선택한 텍스트만 보내는 경로를 테스트한다.
- 구조화 결과는 스키마로 요청하고, 파싱할 수 없는 날짜를 추정해 만들지 않는다.
- 추론 취소, 스트리밍 중단, 모델 keep-alive, 장문 청킹, 출처 표시가 별도 관심사로 구현돼 있다.
- 로컬 Ollama만을 기본 전제로 하므로 외부 전송 경로가 작다.

스핀오프에서는 이 경계를 그대로 유지해야 한다. 반려 AI 페르소나는 메모 내용을 “명령”으로 받아들이기 쉬우므로, 메모·사용자 프로필·외부 첨부를 모두 비신뢰 데이터로 감싸고 액션은 타입이 있는 제안으로만 반환해야 한다.

### 3.4 품질과 테스트

정적으로 확인된 테스트 정의는 356개다. 프롬프트 경계 위조, 구조화 응답 파싱, 스트리밍 순서, 임베딩 실패 폴백, 자동 저장, 크래시 복구, 버전 이력, Markdown 편집, 검색, SQLite 마이그레이션과 벡터 인덱스까지 폭이 넓다. `TreatWarningsAsErrors`, 중앙 패키지 버전 관리, SQLite 안전 버전 점검도 좋은 신호다.

다만 CI가 없으므로 이 테스트들이 매 커밋에서 실제로 실행된다는 보장은 없다. 커버리지 기준, 릴리스 빌드 검증, 코드 서명, 설치·업데이트 경로도 아직 저장소에 드러나지 않는다.

### 3.5 기술 부채와 위험

1. **UI 코드비하인드 비대화**: `NoteWindow.xaml.cs` 약 66KB, `NoteWindowManager.cs` 약 26KB다. 스핀오프 기능을 여기에 계속 추가하면 메모, AI, 컴패니언 상태가 서로 얽힌다. Presenter/ViewModel 또는 기능별 컨트롤로 먼저 분해해야 한다.
2. **조립 루트 비대화**: `App.xaml.cs` 약 12.7KB가 수동 조립과 수명주기를 모두 가진다. DI 패키지 버전은 정의돼 있지만 App 프로젝트는 이를 사용하지 않는다. 새 프로젝트에서 `CompositionRoot`와 백그라운드 서비스 수명주기를 분리할 필요가 있다.
3. **Windows 전용성**: WinUI 3, 전역 핫키, 트레이, Windows App SDK로 인해 Windows가 명확한 제품 범위다. 단기에는 장점이지만 크로스플랫폼을 약속하면 전면 재설계가 된다.
4. **설정 UX 미완성**: 현재 일부 AI 설정은 DB 값을 직접 바꾸는 수준이다. 외부 AI 서버까지 허용하는 스핀오프에는 전송 범위, 모델, 인증서, 보존 기간을 다루는 정식 설정 화면이 필수다.
5. **프로젝트 성숙도**: 최신 커밋 하나가 문서·AI·앱·UI·에셋 등 약 50개 파일을 건드린다. 기능별 PR과 작은 커밋 경계가 필요하다.

## 4. pet_AI_v2 분석

### 4.1 제품과 관계 루프

`pet_AI_v2`의 핵심은 캐릭터 그래픽이 아니라 **관계가 축적되는 규칙**이다.

- 하루 3개의 성장 하트: 함께하기, 필요한 돌봄, 오늘의 부탁
- 결정적인 성격 6종과 카테고리별 숨은 취향
- 발견된 취향만 AI 기억으로 전달
- 하루 부탁은 행동과 방식까지 지정하고 DB에 고정
- 선제 대화는 시간당 상한, 최근 대화 억제, 트리거별 쿨다운을 통과해야 함
- 완벽한 하루의 연속 기록은 보상을 주되, 하루를 놓쳐도 누적 성장과 최고 기록은 사라지지 않음
- AI가 만든 `[ACTION:*]`은 즉시 실행되지 않고 사용자가 누르는 카드가 됨
- AI가 없어도 종별 오프라인 대사가 동작

이 규칙들은 “상주 앱이 귀찮아지지 않는 법”과 “AI가 사용자 행동을 몰래 실행하지 않는 법”을 구현한 좋은 자산이다.

### 4.2 아키텍처

```text
main/App 조립기
 ├─ core: 설정, DB, 모델, 경로
 ├─ domain: 성장, 상태, 감정, 취향, 보상, 업적
 ├─ ai: 프롬프트, OpenAI 호환 클라이언트, 폴백, 액션
 ├─ photo: 수집, 분할, 처리, 앨범, 스타일
 ├─ theme: 토큰, QSS, 공용 컴포넌트
 └─ ui: desktop, chat, photo, collection, settings, onboarding
```

가장 좋은 선택은 [`pet_engine.py`](https://github.com/Ubermensch216/pet_AI_v2/blob/main/todak/domain/pet_engine.py)가 Qt에 의존하지 않고 `Event` 리스트를 반환하는 것이다. UI가 도메인 이벤트를 애니메이션·토스트로 번역하므로 포팅 경계가 분명하다.

AI 계층은 OpenAI 호환 메시지, SSE 스트리밍, 재시도·백오프, 취소 토큰, Ollama 대응, 사고 블록 제거를 갖춘다. 설정된 AI가 실패하면 오프라인 대사로 위장하지 않고 오류를 노출하고, AI가 설정되지 않은 자체 모드에서만 폴백을 쓴다. 사용자 승인형 액션 카드 설계는 스핀오프에 직접 가져갈 가치가 높다.

### 4.3 프라이버시와 오프라인 설계

강점:

- API 키는 Windows 자격 증명 관리자를 우선 사용한다.
- 키링 사용 불가 시 머신·계정 기반 PBKDF2 + AES-GCM 암호문을 DB에 둔다.
- AI 연결이 없으면 대화 경로는 네트워크를 호출하지 않는다.
- 사진 스타일 전송은 명시적 옵트인이고 기본 OFF다.
- 배경 제거 모델은 앱 실행 중 자동 다운로드하지 않으며, 모델이 없으면 GrabCut으로 폴백한다.
- 사진 원본·처리본의 저장 위치와 완전 삭제 동작이 문서화돼 있다.

위험:

1. `ssl_verify` 기본값이 `False`이고 `urllib3` 인증서 경고를 전역 비활성화한다. 행정망 사설 인증서 대응 필요는 이해되지만, 임의의 외부 엔드포인트에도 그대로 적용되면 중간자 공격을 허용한다. 기본은 `True`, 특정 사설 엔드포인트에만 사용자가 경고를 확인하고 예외를 저장하는 방식이 맞다.
2. AES-GCM 폴백 키는 OS 보호 비밀이 아니라 머신 식별자에서 파생한다. “DB 파일만 다른 PC로 복사한 공격”에는 유효하지만, 같은 PC 계정이 탈취된 위협까지 막는 저장소는 아니다. Windows에서는 DPAPI 또는 Credential Manager를 강제하고 폴백의 위협 모델을 명시하는 편이 낫다.
3. 전체 초기화가 테이블 이름을 수동으로 열거한다. 새 마이그레이션에서 테이블 하나를 빼먹으면 “전체 삭제” 약속이 깨질 수 있다. 데이터 소유권 레지스트리나 DB 재생성 전략으로 바꿔야 한다.

### 4.4 품질과 테스트

정적으로 집계된 테스트 정의는 716개다. README의 “417개”와 차이가 있어 문서가 코드보다 뒤처져 있다. 테스트는 AI 태그·프롬프트·폴백, 성장·돌봄·취향·보상, 감정 분석, 사진 EXIF·분할·내보내기, 성능, 온보딩, 장기 여정 등을 폭넓게 다룬다.

하지만 테스트 산출물이 커밋돼 있고 CI가 없다. 테스트 수가 많아도 실행 환경이 깨끗한지, 네트워크 차단 테스트가 실제 패키징 빌드에서도 같은지, 플랫폼별 키링 동작이 검증되는지는 별도다.

### 4.5 기술 부채와 위험

1. **God object 경향**: `database.py` 1,093줄/42KB, `pet_engine.py` 1,000줄/37KB, `chat_window.py` 1,164줄/46KB, `pet_canvas.py` 782줄/29KB다. 도메인 분리는 했지만 각 계층 내부가 다시 거대한 단일 객체로 합쳐졌다.
2. **저장소 오염**: 테스트 DB·이미지 252개가 추적 중이다. 빌드·검토 속도, 민감 데이터 위험, Git 이력 크기에 악영향을 준다.
3. **문서-동작 불일치**: README는 연결이 끊겨도 종별 대사를 한다고 읽히지만, 현재 클라이언트는 AI가 명시적으로 설정된 상태의 실패를 오류로 노출하고 폴백하지 않는다. 이것은 좋은 제품 결정일 수 있으나 문구를 맞춰야 한다.
4. **설정 허용 목록 무력화**: `Config.load()`의 `if k in DEFAULTS or True`는 모든 키를 받아들인다. 의도된 확장성이라면 조건이 필요 없고, 허용 목록을 의도했다면 명백한 버그다.
5. **식별자 취약성**: `born_at` 타임스탬프가 펫별 데이터의 실질 키이고 충돌 시 1초를 더한다. 새 프로젝트에서는 UUID `companion_id`를 사용해야 한다.
6. **범위 과다**: 사진 처리, 성장 앨범, 7종, 378 스프라이트, 90업적, 보상 상점, 감정 히트맵, 장기 명부를 한 번에 유지한다. 새 제품의 핵심 가치를 검증하기 전에 이 범위를 그대로 가져오면 개발 속도가 급감한다.

## 5. 결합 원칙

### 5.1 가져갈 것

| 출처 | 자산 | 방식 |
|---|---|---|
| deskNote | Core 계약·정책, SQLite 마이그레이션, 저장·복구·이력 | 직접 기반으로 사용 |
| deskNote | FTS5 + 임베딩 + RRF, 청킹, 출처 표시 | 직접 사용 후 컴패니언 검색 범위 추가 |
| deskNote | AI 미리보기·적용, 비신뢰 노트 경계 | 제품 불변식으로 유지 |
| pet_AI_v2 | 순수 도메인 이벤트 모델 | C#으로 포팅 |
| pet_AI_v2 | ProactiveGuard의 3중 빈도 제한 | C#으로 포팅 |
| pet_AI_v2 | 결정적 성격·발견된 기억만 프롬프트에 포함 | 컴패니언 프로필에 맞게 포팅 |
| pet_AI_v2 | 스트리밍 취소, 재시도, 사용자 승인 액션 카드 | deskNote AI 인터페이스에 통합 |
| pet_AI_v2 | 디자인 토큰과 캐릭터 표현 규칙 | WinUI 리소스로 재작성 |

### 5.2 초기에는 가져오지 않을 것

| 항목 | 이유 |
|---|---|
| Python 런타임·PySide6 UI | 이중 런타임/패키징/창 수명주기 복잡성 |
| 두 번째 SQLite DB | 트랜잭션·백업·삭제·마이그레이션 일관성 저하 |
| 사진 분할·성장 앨범 | 핵심 메모리 루프와 무관하고 개인정보 면적이 큼 |
| 90개 업적·보상 상점 | 행동 조작 위험과 밸런싱 비용 |
| 7종 전체·잠금 해제 | 콘텐츠 제작 비용이 제품 검증을 가림 |
| 밥·청결 감쇠 | 생산성 앱에서 죄책감과 알림 피로를 유발 |

## 6. 권장 제품: Ambient Memory Companion

### 6.1 핵심 사용자 루프

```text
빠르게 기록
  ↓
로컬 저장·태그·임베딩
  ↓
컴패니언이 관련 맥락을 조용히 발견
  ↓
브리핑·리마인드·관련 메모를 출처와 함께 제안
  ↓
사용자가 열기/적용/무시를 선택
  ↓
기록 → 회상 → 완료가 관계의 기억으로 축적
```

하루 세 슬롯은 다음처럼 재해석하는 것이 좋다.

- **기록**: 새 메모 또는 기존 메모에 의미 있는 내용 추가
- **회상**: 과거 메모를 다시 열고 관련 맥락 확인
- **정리**: 체크리스트 완료, 알림 처리, AI 제안 승인 중 하나

이 슬롯은 보상을 위한 강제 과제가 아니라 “오늘 이 앱이 실제로 도움을 줬는가”를 설명하는 회고 지표다. 빈 날에는 관계가 줄지 않고, 컴패니언도 아프거나 서운해하지 않는다.

### 6.2 MVP 기능

1. 기존 deskNote의 메모·자동 저장·이력·검색·알림
2. 화면 가장자리의 작은 컴패니언 1종과 접기/끄기
3. “오늘의 기억” 브리핑: 오늘 알림, 미완료 체크, 최근 수정, 오래 묵은 관련 메모
4. 메모에 질문하기 + 사용 근거 메모 표시
5. 컴패니언 선제 제안 4종: 알림 임박, 관련 메모, 오래된 미완료, 아침 브리핑
6. 시간당 상한·조용한 시간·최근 상호작용 억제
7. 액션 카드: 메모 열기, 알림 만들기, 체크리스트 제안, 요약 제안. 모두 클릭 후 실행
8. 로컬 Ollama 기본, 외부 OpenAI 호환 서버는 명시적 옵트인

### 6.3 MVP 이후

- 컴패니언 성격 3종과 사용자가 발견한 선호
- 주간 회고와 기억 타임라인
- 실제 반려동물 사진 기반 아바타
- 기기 간 암호화 동기화
- 확장 가능한 provider/skill 시스템

## 7. 권장 기술 구조

```text
SpinOff.App (WinUI 3)
 ├─ Notes UI / Companion overlay / Chat / Settings
 ├─ CompositionRoot / Hosted background services
 └─ ViewModels + feature-scoped controllers

SpinOff.Core
 ├─ Notes domain (기존 deskNote)
 ├─ Companion domain
 │   ├─ CompanionProfile
 │   ├─ RelationshipJournal
 │   ├─ DailyRitual
 │   ├─ ProactivePolicy
 │   └─ SuggestionPolicy
 └─ Typed domain events

SpinOff.Data
 ├─ Notes repositories / FTS5 / vectors
 ├─ Companion repositories
 └─ One SQLite DB, versioned migrations

SpinOff.Ai
 ├─ IAiProvider
 ├─ OllamaProvider
 ├─ OpenAiCompatibleProvider
 ├─ Retrieval + prompt firewall
 └─ Typed suggestions, streaming, cancellation
```

권장 신규 핵심 식별자와 테이블:

- `companion_profiles(companion_id UUID, persona, created_at, enabled, ...)`
- `relationship_events(id, companion_id, event_type, note_id?, occurred_at, payload_json)`
- `daily_rituals(companion_id, date, capture, recall, resolve)`
- `proactive_history(companion_id, trigger, fired_at, acted_at?, dismissed_at?)`
- `ai_suggestions(id, type, source_note_ids, payload, status, created_at, expires_at)`
- `consent_grants(scope, provider_id, granted_at, revoked_at?)`

`relationship_events`에는 메모 본문 전체를 복제하지 말고 note ID와 최소 메타데이터만 둔다. 본문은 기존 노트 저장소가 단일 원본이어야 삭제·복원·백업이 일관된다.

## 8. 반드시 지킬 불변식

1. AI와 컴패니언을 꺼도 메모 기능은 100% 동작한다.
2. 메모 본문은 데이터이며 시스템 지시가 아니다.
3. AI가 메모·알림·체크리스트를 자동 변경하지 않는다. 항상 제안 → 사용자 승인이다.
4. 모든 AI 답변은 사용한 메모 근거를 확인할 수 있다.
5. 선제 대화에는 시간당 상한, 동일 트리거 쿨다운, 최근 대화 억제, 조용한 시간이 있다.
6. 생산성이 낮은 날 관계 수치가 감소하거나 캐릭터가 아프지 않는다.
7. 외부 AI로 보내는 데이터 범위는 호출 전에 사용자가 알 수 있다.
8. “전체 삭제”는 새 스키마가 추가돼도 누락되지 않는 자동 검증 테스트를 가진다.
9. 저장·임베딩·AI·컴패니언 작업은 앱 시작과 메모 입력을 막지 않는다.
10. 한 DB의 백업·복원으로 메모와 컴패니언 관계가 함께 복원된다.

## 9. 실행 로드맵

### Phase 0 — 저장소 정비 (2~3일)

- 새 저장소와 제품 코드네임 결정
- LICENSE와 공개/비공개 정책 결정
- `pet_AI_v2` 테스트 산출물 추적 중단 및 민감성 확인
- GitHub Actions에 빌드·테스트·포맷·패키징 smoke test 추가
- Conventional Commits 또는 최소한의 변경 의도 규칙 적용
- 두 저장소의 현재 테스트를 기준선으로 기록

완료 기준: 깨끗한 checkout에서 한 명령으로 빌드·테스트가 성공하고, 산출물이 Git 상태를 더럽히지 않는다.

### Phase 1 — deskNote 기반 안정화 (3~5일)

- `NoteWindow.xaml.cs`, `NoteWindowManager`, `App.xaml.cs` 분해
- 노트 저장·검색·알림에서 typed domain event 발행
- 정식 Settings UI 뼈대 추가
- AI provider 계약을 Ollama 단일 구현에서 확장 가능하게 정리

완료 기준: 기존 메모 UX와 356개 테스트가 유지되고, 컴패니언 없이도 기존 기능이 동일하다.

### Phase 2 — 최소 컴패니언 도메인 (5~7일)

- `companion_id` 기반 프로필, 관계 이벤트, 일일 리추얼
- `ProactivePolicy` 포팅
- 캐릭터 1종, 3~4개 표정, 접기/끄기
- 메모 행동을 관계 이벤트로 투영하되 본문 복제 금지

완료 기준: UI 없이 순수 Core 테스트로 모든 규칙을 검증할 수 있다.

### Phase 3 — 메모리 브리지 (5~8일)

- 오늘 브리핑과 관련 메모 제안
- 근거 note ID가 있는 typed AI suggestion
- 액션 카드 승인 흐름
- 로컬 Ollama + 외부 OpenAI 호환 provider, 전송 동의 UI

완료 기준: 컴패니언 제안 하나가 생성 → 근거 확인 → 승인/거절 → 감사 로그까지 이어진다.

### Phase 4 — UX 검증과 패키징 (5~7일)

- 알림 피로, 조용한 시간, 키보드 전용 흐름
- 강제 종료·절전 복귀·DB 마이그레이션·모델 미설치 테스트
- Windows x64 self-contained 패키지와 서명 계획
- 10~20명 소규모 사용성 검증

성공 지표:

- 메모 생성 시간과 자동 저장 신뢰도가 기존 deskNote보다 나빠지지 않음
- 선제 제안 무시율과 “방해됨” 응답이 설정 임계치 이하
- 브리핑 또는 관련 메모 제안에서 실제 메모 재방문이 발생
- AI를 끈 사용자도 핵심 가치를 느낄 수 있음

## 10. 우선순위 위험표

| 우선순위 | 위험 | 대응 |
|---|---|---|
| P0 | 두 UI/런타임을 합쳐 패키징과 상태가 이원화됨 | deskNote 기반 단일 .NET 앱, pet 도메인만 포팅 |
| P0 | 펫의 감쇠가 생산성 죄책감으로 바뀜 | 관계 감소 제거, 긍정적 누적만 사용 |
| P0 | 외부 AI에 노트가 암묵적으로 전송됨 | 로컬 기본, scope별 명시적 동의, 호출 감사 로그 |
| P0 | 테스트 산출물·사진이 Git에 남음 | 추적 중단, 민감성 검사, 필요 시 이력 정리 |
| P1 | 거대 code-behind와 God object가 기능 결합을 가속 | 기능별 ViewModel/서비스/리포지토리 분리 |
| P1 | SSL 검증 기본 OFF | 기본 ON, 엔드포인트별 경고 기반 예외 |
| P1 | 문서와 코드 동작 불일치 | CI에서 문서 수치 제거 또는 자동 생성 |
| P1 | CI·릴리스·서명 부재 | Phase 0에서 자동화와 배포 기준선 구축 |
| P2 | 캐릭터 콘텐츠 제작이 MVP를 지연 | 1종/소수 표정으로 검증 후 확장 |
| P2 | 관계 이벤트가 메모 본문을 중복 저장 | ID·메타데이터만 저장, 본문 단일 원본 유지 |

## 11. 최종 권고

새 프로젝트는 `deskNote`를 fork한 뒤 이름만 바꾸는 방식보다, **deskNote의 Core/Data/Ai 구조를 보존한 새 저장소에 App/UI를 점진적으로 재구성하는 형태**가 안전하다. 법적 라이선스가 아직 없고 두 저장소 모두 본인 소유 비공개 저장소이므로 내부 스핀오프는 가능하지만, 외부 공개 전에는 라이선스와 제3자 에셋 출처를 확정해야 한다.

첫 구현 목표는 “펫이 움직인다”가 아니라 다음 세 문장이 실제로 성립하는지 검증하는 것이다.

1. 사용자는 기존처럼 즉시 메모할 수 있다.
2. 컴패니언은 과거 메모를 근거와 함께 적절한 순간에 되돌려준다.
3. 사용자는 모든 자동화의 최종 결정권을 가진다.

이 셋이 성립하면 `pet_AI_v2`의 성격·취향·사진·수집 요소는 강력한 확장 자산이 된다. 성립하기 전에 그 콘텐츠를 먼저 옮기면 스핀오프는 “귀여운 기능이 많은 두 번째 앱”이 되고, 성립한 뒤 옮기면 “사용자의 기억과 함께 자라는 새로운 제품”이 된다.
