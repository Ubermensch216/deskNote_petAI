# DeskNote

Windows 로컬 AI 스티키 위젯. 제품 정의는
[기획 보고서](docs/Windows%20로컬%20AI%20스티키%20위젯%20제품·기술%20기획%20보고서.pdf)를 따른다.

핵심 원칙은 보고서 p21의 우선순위다: **메모 → 기억 → AI → 자동화**.
AI는 어느 단계에서도 메모 앱의 필수 의존성이 되지 않는다. 모델을 받지 않아도 메모 기능은
100% 동작하고, 모델을 지워도 데이터는 온전하다.

## 프로젝트 구조

| 프로젝트 | 대상 | 역할 |
|---|---|---|
| `src/DeskNote.Core` | net10.0 | 도메인 모델, 인터페이스, UI·DB에 의존하지 않는 정책 |
| `src/DeskNote.Data` | net10.0 | SQLite + FTS5, 마이그레이션, 리포지토리 |
| `src/DeskNote.App` | net10.0-windows | WinUI 3 데스크톱 UI (실행 진입점) |
| `tests/DeskNote.Core.Tests` | net10.0 | 도메인·배치·자동저장 정책 |
| `tests/DeskNote.Data.Tests` | net10.0 | 영속성, 한국어 FTS, 마이그레이션 |
| `tests/DeskNote.Benchmarks` | net10.0 | SLO 측정과 강제 종료 내성 검증 (콘솔 실행 파일) |

의존 방향은 `App → Core ← Data`. `Core`는 아무것도 참조하지 않는다.
AI 계층은 `Core`의 `ILocalAiService` 뒤에만 존재한다. 구현은 `src/DeskNote.Ai`의
`OllamaAiService`이고, 로컬 모델이 없거나 `ai.enabled` 가 꺼져 있으면 항상 "사용 불가"를
반환하는 `NullAiService`로 내려간다.

## 빌드와 실행

```bash
dotnet build
```

```bash
dotnet run --project src/DeskNote.App
```

Visual Studio는 필요 없다. WinUI 3의 XAML 컴파일러와 Windows SDK 빌드 도구는 NuGet에서 온다.
앱은 unpackaged + Windows App SDK self-contained로 빌드되므로 별도 런타임 설치도 필요 없다.

## 테스트

```bash
dotnet test
```

xunit.v3 는 Microsoft.Testing.Platform 러너를 직접 호스팅하고 .NET 10 SDK 는 VSTest 로 그것을 실행하지
않는다. 그래서 `global.json` 의 `test.runner` 로 러너를 지정해 둔다 — 이 항목이 없으면 `dotnet test` 는
바로 오류로 끝난다. 현재 `Core`·`Data`·`Ai` 470개 테스트가 모두 통과한다.

## 편집 단축키

| 키 | 동작 |
|---|---|
| `Ctrl+B` / `Ctrl+I` | 굵게 / 기울임 |
| `Ctrl+U` / `Ctrl+Shift+X` | 밑줄 / 취소선 |
| `Ctrl+Shift+K` | 인라인 코드 |
| `Ctrl+K` | 링크 |
| `Ctrl+1` / `Ctrl+2` / `Ctrl+3` | 제목 1·2·3 |
| `Ctrl+Shift+L` | 글머리 목록 |
| `Ctrl+Shift+C` | 체크리스트 |
| `Ctrl+Enter` | 커서 줄의 체크박스 토글 |
| `Enter` | 목록 안에서 다음 항목 이어쓰기 (빈 항목에서는 목록 종료) |

메모 본문은 Markdown 원문 그대로 저장된다. 체크리스트도 본문 안의 `- [ ]` 가 유일한 원본이며,
`checklist_items` 테이블은 저장 시 본문에서 다시 만들어지는 조회용 투영이다. 밑줄만은 Markdown 문법이
없어 `<u>…</u>` 로 쓴다 — 마커를 새로 발명하면 이 앱을 벗어나는 순간 의미를 잃는다.

## 메모 모양

쉬고 있는 노트에는 아무 조작 요소도 보이지 않는다. 화면에 있는 것은 글과 그 글이 놓인 종이뿐이고,
포인터가 올라오거나 키보드 포커스가 들어오면 그때 조작 요소가 페이드인한다 (보고서 p4). 자리는 미리
잡아 두므로 나타났다 사라질 때 본문이 다시 흐르지 않는다.

| 위치 | 내용 |
|---|---|
| 위쪽 | `+` 새 메모 · `✨ AI` · `⋯` 메모 설정 · `✕` 닫기, 그 사이는 창을 끄는 손잡이 |
| 아래쪽 | 굵게 · 기울임 · 밑줄 · 취소선 · 글머리 목록 · 체크리스트 · 이미지 |
| `✨ AI` 메뉴 | 다듬기(요약 · 정리) · 재작성(간결 · 격식 · 친근 · 보고서) · 뽑아내기(할 일 · 태그) · 질문 |
| `⋯` 메뉴 | 색상 8종 · 불투명도 · 크기 · 알림 · 항상 위 · 메모 라이브러리 · 기록 · 삭제 |

메뉴는 둘로 나뉜다. `⋯` 는 **메모라는 물건에 대한 것** 이고 — 무슨 색인지, 얼마나 큰지, 언제
말을 거는지, 어디에 있는지 — `✨ AI` 는 **본문에 대한 것** 이다. AI가 이 앱의 제품이므로 색상
여덟 개 아래로 스크롤해야 나오는 한 구역이 아니라 제 버튼을 갖는다.

두 메뉴는 같은 구조를 쓴다. **왼쪽 26pt 세로줄이 카테고리** 이고, 그 오른쪽에 있는 것이 전부 그
카테고리에 속한다. 세로줄만 내려 읽으면 타일을 하나도 읽기 전에 그 메뉴가 무엇을 하는지 알 수
있다.

| 메뉴 | 세로줄을 내려 읽으면 |
|---|---|
| `✨ AI` | 다듬기 · 재작성 · 뽑아내기 · 질문 |
| `⋯` | 불투명도 · 크기 · 알림 · 항상 위 · 라이브러리 · 기록 · 삭제 |

카테고리를 세로줄에 넣기 전에는 `재작성`·`뽑아내기` 가 각각 제 행을 하나씩 차지하는 caption
이었다. 같은 말을 하면서 행을 세 개 더 쓰는 셈이고, 180pt 노트에는 그럴 여유가 없다.

항목은 글자보다 아이콘이 먼저 오게 배치했다. 아이콘 아래의 이름은 남겨 둔다 — 깔때기가 `요약`
이라는 것은 알고 나서야 읽히고, 이름은 아이콘을 가르치는 쪽이며 그 다음부터 메뉴를 훑게 만드는
것은 아이콘이다. `10분`·`1시간`·`내일` 처럼 아이콘이 대신할 수 없는 값은 글자로 남겼다.

메뉴는 열릴 때 노트 크기에 맞춰진다. 패키징하지 않은 WinUI 팝업은 제 창 밖으로 나가지 못하고
잘려 나가므로, 세로는 스크롤로 접고 가로는 폭을 줄여 색상 줄이 두 줄로 감긴다. 180pt 노트에서도
마지막 줄까지 누를 수 있어야 메뉴다.

색상·불투명도·항상 위·크기는 모두 노트마다 저장되므로 다시 켜도 그대로 뜬다. 불투명도에는
하한(`Note.MinOpacity`)이 있어 노트를 완전히 투명하게 만들어 잃어버리는 일은 없다. 아래쪽 서식
막대의 명령은 전부 위의 단축키와 같은 것이며, 막대는 그것을 찾을 수 있게 하고 단축키는 빠르게 한다.

제목 칸은 없다. 스티커 메모는 빈 창에 떠오른 생각을 적는 자리라 제목 칸은 거의 언제나 비어 있고,
240×180 노트에서 그 한 줄은 비싸다. 제목은 본문의 첫 줄에서 만들어지며(`NoteContent.DeriveTitle`),
`# 회의 준비` 는 `회의 준비` 가 된다 — 마크업은 그 줄이 무슨 말인지가 아니라 어떻게 보이는지다.

로컬 모델이 없으면 `✨ AI` 메뉴의 타일은 전부 비활성이고 그 자리에 이유가 적힌다. 누를 것이 없다는
사실을 UI에서 그대로 보여주는 편이, 눌렀을 때 실패하는 버튼보다 낫다.

## 메모 라이브러리

노트의 `⋯` 메뉴에서 **메모 라이브러리 / Notes Explorer** 를 연다. 전체 메모를 노트북·태그·삭제
여부로 좁혀 보고, 검색창에 입력하면 150ms 뒤 검색이 돈다. 행을 한 번 누르면 해당 메모가
바탕화면에 뜬다.

검색은 **두 번 그린다**. 키워드(FTS) 결과가 먼저 뜨고, 약 0.3초 뒤 의미 검색 결과가 도착하면
융합 순위로 다시 그린다. 순서가 바뀌지 않으면 다시 그리지 않으므로, 첫 패스에서 찾은 사용자는
두 번째 패스가 있었다는 것도 모른다. 임베딩 모델이 없으면 두 번째 패스는 아무것도 반환하지 않고
검색은 예전 그대로다 (`HybridLibrarySearch`).

```
검색어: "공직자 임명"   (메모 본문: "장관 후보자 6명 지명 … 국가 AI 전략 총괄 인선")
FTS 결과 0건 · 벡터 결과 1건 → 융합 결과 1건
```

의미 검색은 노트북·태그·삭제 필터를 모른다. 그래서 융합된 순위는 `ListByIdsAsync` 를 거쳐
**순서는 순위에서, 소속은 필터에서** 가져온다 — 벡터가 좋아했지만 지금 필터가 배제한 메모는
사이드바가 부정하는 결과로 보이는 대신 여기서 빠진다.

**삭제됨** 범위에서는 행을 고르고 **복원 / Restore** 를 누른다. 삭제는 소프트 삭제이므로 되돌릴 수 있다.
왼쪽의 **새 노트북** 으로 노트북을 만든다.

태그는 본문 안의 `#backend` 처럼 쓴 해시태그가 원본이고, `tags`/`note_tags` 테이블은 저장 시
본문에서 다시 만들어지는 조회용 투영이다. Markdown 제목(`# 제목`)과 구분하기 위해 `#` 바로 뒤에
공백이나 `#` 이 오면 태그로 보지 않는다.

## 알림

노트의 `⋯` → **알림 / Remind me** 에서 10분 뒤·1시간 뒤·내일 아침 9시를 고른다. 30초마다
확인하므로 절전/최대 절전에서 깨어난 뒤에도 놓치지 않는다. 반복 알림은 RFC 5545 `RRULE` 의
부분집합(`FREQ=DAILY|WEEKLY|MONTHLY|YEARLY`, `INTERVAL`)을 쓰며, 자리를 비운 사이 지나간 반복은
하나로 합쳐 다음 회차만 알린다.

## 첨부

이미지를 붙여넣으면 메모 폴더로 복사되고, 파일을 끌어다 놓으면 10MB 이하 이미지만 복사하고
나머지는 원본 위치를 링크한다 (보고서 p5: 대용량은 복사보다 링크 우선). 서식 막대의 이미지 단추는
파일 선택 대화상자를 열어 같은 경로를 탄다 — 붙여넣기와 끌어다 놓기는 빠르지만 어느 쪽도 눈에 보이지
않는다. 어느 쪽이든 본문에는 Markdown 참조가 삽입되므로 `Ctrl+Z` 로 되돌릴 수 있다.

## 전역 단축키와 트레이

| 기본 키 | 동작 |
|---|---|
| `Ctrl+Alt+N` | 새 메모 |
| `Ctrl+Shift+F` | 메모 라이브러리 열기 |
| `Ctrl+Shift+P` | 마지막으로 쓰던 메모의 항상 위 토글 |
| `Ctrl+Shift+R` | 마지막으로 쓰던 메모에 1시간 뒤 알림 |

네 개 모두 `settings` 테이블의 `input.hotkeys` (명령 이름 → 제스처 JSON) 로 바꿀 수 있다. 전용 설정
화면은 아직 없다. 다른 앱이 이미 그 조합을 쓰고 있어 등록에 실패하면 조용히 넘기지 않고 로그에
남긴다. `Ctrl+Space`(AI 명령 팔레트)는 보고서에 있지만 **기본 바인딩에 넣지 않았다** —
아직 호출할 AI 계층이 없어 다른 앱에서 조합만 빼앗게 되기 때문이다.

트레이 아이콘은 더블클릭으로 새 메모, 우클릭으로 새 메모 · 메모 라이브러리 · Windows 시작 시 실행 ·
종료를 제공한다. 마지막 메모를 닫아도 여기로 돌아올 수 있다.

## 언어

UI 문자열은 `src/DeskNote.App/Strings/{ko-KR,en-US}/Resources.resw` 에 있다. 기본은 Windows 로케일을
따르고, `settings` 의 `ui.locale` 값으로 수동 전환한다. 하드코딩된 한국어 UI 문자열이 하나라도 생기면
`LocalizationGuardTests` 가 실패한다.

## 로컬 AI

AI는 [Ollama](https://ollama.com)를 통해 로컬에서만 돈다. `ILocalAiService` 뒤의 구현이
`src/DeskNote.Ai`의 `OllamaAiService`이고, 인터페이스가 요구하는 "모델은 별도 프로세스에서"
격리는 Ollama 자체가 별도 프로세스라 그대로 충족된다. 데몬이 죽으면 HTTP 호출이 실패해 AI가
"사용 불가"로 내려갈 뿐, 메모 창은 아무 영향도 받지 않는다.

```bash
ollama pull gemma4:e2b
```

| 설정 키 | 기본값 | 뜻 |
|---|---|---|
| `ai.enabled` | (없음 = 켬) | `false` 면 소켓조차 열지 않는다 |
| `ai.endpoint` | `http://localhost:11434` | 로컬 추론 서버 주소 |
| `ai.model` | `gemma4:e2b` | 텍스트 동작에 쓰는 모델 |
| `ai.embeddingModel` | `bge-m3:latest` | 검색 임베딩용 모델 |
| `ai.keepAliveMinutes` | `15` | 모델을 메모리에 유지하는 시간 (0~60분, `0` 이면 즉시 내림) |

값이 깨져 있으면 기본값으로 되돌아간다. AI 설정을 잘못 써서 앱이 못 뜨는 일은 없다.

가용성 확인(`/api/tags`)은 메모가 화면에 뜬 뒤 백그라운드에서 시작하고 1분마다 다시 확인한다.
가중치는 이 경로에서 절대 로드하지 않는다 (보고서 p14: "앱 시작 = 모델 시작" 금지).

메모 본문은 **데이터이지 지시가 아니다**. 본문·선택 영역·제목은 언제나 시스템 프롬프트가 아니라
사용자 메시지의 `<UNTRUSTED_NOTE_CONTEXT>` 블록 안에 들어가고, 본문이 그 구분자를 흉내 내면
지워진다 (보고서 p16). 할 일 추출과 태그 제안은 JSON 스키마로 디코딩을 강제한 뒤 한 번 더
검증한다 — 읽을 수 없는 날짜는 추측하지 않고 버린다 (보고서 p11).

i7 / 16GB CPU 추론에서 gemma4:e2b 기준 요약 약 25초, 할 일 추출 약 45초가 걸린다. 모델이 메모리에
올라가 있지 않은 첫 호출은 7.2GB를 읽어 들이느라 60초에 가깝다. 모든 동작은 취소 가능하고,
결과는 적용 전까지 메모에 쓰이지 않는다.

그 60초를 두 번 내지 않기 위해 두 가지를 한다. 모든 요청에 `keep_alive` 를 실어 보내
(`ai.keepAliveMinutes`, 기본 15분) Ollama 기본값인 5분 유휴 언로드를 늘리고, **`✨ AI` 메뉴가
열리는 순간** 빈 메시지 요청으로 모델을 미리 올린다. 사용자가 타일을 고르는 몇 초가 로딩과
겹치는 셈이다. 예열은 순수한 최적화라 실패해도 아무것도 보고하지 않는다 — 데몬이 죽어 있으면
타일은 이미 비활성이고, 살아나면 다음 프로브가 알아챈다. `keep_alive` 는 요청마다 보낸다:
Ollama 는 마지막으로 받은 값을 적용하므로 한 번이라도 빠뜨리면 기본값으로 돌아간다.

### 쓰는 법

노트 위쪽의 **`✨ AI`** 버튼에서 고른다. 텍스트를 선택해 두면 그 부분만, 선택이 없으면
메모 전체가 대상이다.

| 타일 | 동작 |
|---|---|
| 요약 / 정리 | 핵심만 남기기 / 제목·목록·체크박스로 구조화 |
| 간결 · 격식 · 친근 · 보고서 | 같은 내용을 그 어조로 다시 쓰기 |

누르면 미리보기 창이 **먼저** 뜨고 (CPU에서 수십 초가 걸리므로 기다리는 중임을 보여준다),
끝나면 원본과 제안을 나란히 보여준다. **적용**을 눌러야 본문이 바뀌고, 그 편집은 일반 편집과
똑같이 `Ctrl+Z` 로 되돌아가며 리비전으로 남는다. 창을 닫으면 추론이 취소된다.

### 결과가 메모에 들어가는 모양

메모는 글꼴 하나짜리 평범한 텍스트 상자다. `**중요**` 를 굵게 그려 주는 것이 없으므로 그 별표
네 개는 서식이 아니라 눈이 건너뛰어야 할 문장 부호다. 그래서 모델의 답은 사용자에게 보이기 전에
**메모가 실제로 그릴 수 있는 것만 남기고** 정리된다 (`AiTextCleanup`).

| | |
|---|---|
| 남는 것 | `- ` 목록 · `- [ ] ` 체크박스 · `1. ` 번호 · `#태그` · URL |
| 없어지는 것 | `**굵게**` · `*기울임*` · `` `코드` `` · `~~취소선~~` · `# 제목` · 표 · 코드 블록 · 구분선 |

남기는 쪽은 취향이 아니라 기능이다. 체크박스는 눌러서 토글되고, 목록은 Enter로 이어지고,
해시태그는 저장할 때 `tags` 테이블로 투영된다 — 이것들을 지우면 메모를 정돈하는 게 아니라
망가뜨린다. 소제목은 기호를 뗀 한 줄로 남는데, 크기가 하나뿐인 화면에서 `##` 는 "여기서 글자가
커졌어야 했다" 는 말밖에 하지 못하기 때문이다.

정리는 **미리보기에 보여 주기 전에** 돌린다. 사용자가 승인한 문장과 메모에 들어간 문장이 다르면
미리보기가 존재할 이유가 없다. 할 일 추출의 제목도 같은 처리를 거치므로, 확인 목록에 보이는 줄이
곧 메모에 붙는 줄이다.

프롬프트도 같이 고쳤다. 모델에게 평문으로 답하라고 먼저 부탁하고, 그래도 남는 것을 위 정리가
받아낸다 — 작은 로컬 모델이 가장 먼저 잊는 것이 서식 지시이고, 그 사실을 메모에서 확인할 수는
없기 때문이다. 짝이 맞는 표시만 걷어내므로 `2 * 3` 이나 `note_repository` 는 그대로 살아남는다.

모델이 답하는 동안 메모를 고쳤다면, 적용 대상 구간이 더 이상 같지 않으므로 적용을 거부하고
그 사실을 알린다 — 사용자가 방금 쓴 문장을 오래된 제안으로 덮어쓰지 않는다.

AI가 준비되지 않았으면 타일이 비활성화되고 메뉴 맨 아래에 이유가 표시된다 (모델 없음 / 서버
미실행 / 꺼짐). 준비됐으면 모델 이름과 실행 위치(CPU·GPU)가 같은 자리에 표시된다.

### 할 일 추출과 태그 제안

AI 메뉴 **뽑아내기** 의 `할 일` · `태그` 는 본문을 고쳐 쓰는 대신 **목록을 제안**한다. 모델은 세 개는 맞히고
하나는 틀릴 수 있으므로, 항목마다 체크박스가 따로 붙는다. 기본은 전부 선택돼 있고, 적용은 선택한
것만 반영한다.

- 할 일 → 본문 끝에 `- [ ] 제목` 으로 붙는다. 마감이 있으면 그 시각에 이 메모의 알림이 걸린다.
  마감·담당·중요도는 확인 목록에서 보여 주되 본문 줄에는 넣지 않는다 — 본문은 사용자의 문장이다.
- 태그 → 본문 끝에 `#태그` 로 붙는다. 이미 달려 있는 태그는 제안 목록에서 빠진다.

뽑아낼 것이 없으면 빈 목록 대신 "제안할 것이 없습니다" 라고 말한다. 마감이 적히지 않은 할 일에
날짜를 지어내지 않는 것과 같은 이유다.

### 메모 Q&A

`Ctrl+Space` (또는 AI 메뉴 맨 아래의 `질문`) 로 사이드카를 연다. 마지막으로 쓰던 메모가 있으면 그 메모만
근거로 답하고, 없으면 하이브리드 검색으로 관련 메모를 찾아 답한다 — 어느 쪽인지 창 위에 적혀 있다.
답은 토큰이 오는 대로 흘러나오고, 창을 닫으면 추론이 취소된다. 이 창은 메모를 고치지 않는다.

답 아래에 **근거 메모** 가 붙는다. 답이 오기 전에 먼저 뜨므로 수십 초를 기다리는 동안 무엇을
근거로 쓰는지 보이고, 누르면 그 메모가 바탕화면에 뜬다. 검색을 두 번 돌리지는 않는다 — 창이 먼저
검색해서 찾은 메모 id 를 `ScopedNoteIds` 로 넘기면 서비스는 그 메모들만 읽는다. 이 크기의 로컬
모델은 틀리며, 정확도를 올리는 것보다 **틀렸는지 확인할 수 있게 하는 편** 이 현실적이다.

`Ctrl+Space` 는 **`ai.enabled` 가 꺼져 있지 않을 때만** 등록한다. 한국어 IME도 원하는 조합이라,
호출할 것이 없는 상태에서 시스템 전역으로 뺏지 않는다.

### 의미 검색 (하이브리드 검색)

Q&A가 메모를 고를 때 키워드 검색과 의미 검색을 함께 돌린다. 둘은 서로 다른 방식으로 실패한다 —
FTS는 "페이먼트 모듈 논의"라고 적힌 메모를 "결제 관련 회의"로 찾지 못하고(겹치는 단어가 없다),
임베딩은 주문번호처럼 질문 문장으로 옮기기 어려운 내용을 놓친다. 실측:

```
Q: "결제 시스템은 언제 손보기로 했어?"   (메모 본문: "페이먼트 모듈 리팩터링을 3분기에")
FTS 결과 0건 · 벡터 결과 1건 → 융합 결과 1건
```

순위 융합은 RRF(Reciprocal Rank Fusion)를 쓴다. FTS5 rank는 음수 BM25이고 벡터는 코사인이라
같은 축에서 비교할 수 없지만 순위는 비교할 수 있다. 양쪽 모두에서 나온 메모가 한쪽에서만 나온
메모를 이긴다 — 하이브리드를 쓰는 이유가 그것이다.

| 조각 | 위치 |
|---|---|
| 청킹 (빈 줄 기준, 500자, 80자 겹침) | `Core/Services/NoteChunker.cs` |
| 순위 융합 | `Core/Services/RankFusion.cs` |
| 임베딩 호출 (`/api/embed`) | `Ai/OllamaEmbeddingService.cs` |
| 벡터 저장·검색 | `Data/SqliteVectorIndex.cs` (`002_embeddings.sql`) |
| 저장 뒤 색인 | `App/Services/EmbeddingIndexer.cs` |
| 키워드+의미 결합 | `App/Services/HybridRetriever.cs` |

보고서(p8)는 sqlite-vec을 지목하면서 같은 문단에서 pre-v1이라 파괴적 변경 가능성을 경고한다.
그래서 실제로 넣은 건 **평범한 SQLite 테이블에 float32 BLOB을 넣고 코사인을 in-process로 도는
브루트포스**다. 아키텍처별 네이티브 확장을 싣고 업그레이드마다 재검증하는 비용이, 수천 개 벡터를
몇 밀리초에 훑는 이득보다 크다. `IVectorIndex` 뒤에 있으므로 그 비용이 정당해지는 날 바뀌는 파일은
`SqliteVectorIndex.cs` 하나다.

임베딩은 **저장의 결과이지 저장의 일부가 아니다**. 저장은 그대로 30ms 안에 끝나고
(보고서 p13), 벡터는 잠시 뒤 따라온다. 임베딩 모델이 없으면 아무 오류도 내지 않고 그 메모가 의미
검색에서 빠질 뿐이다. 같은 메모를 연달아 고치면 큐에서 하나로 합쳐진다.

모델 이름이 행마다 저장되므로, `ai.embeddingModel` 을 바꾸면 이전 모델의 벡터는 비교 대상에서
빠지고 "아직 색인 안 됨"으로 취급돼 다시 만들어진다. 서로 다른 모델의 벡터를 섞어 비교하는 일은
없다. 시작할 때마다 색인이 없는 메모를 25개씩 따라잡는다.

bge-m3는 1024차원이고, 질문 하나를 임베딩하는 데 약 0.3초가 걸린다(모델이 올라가 있을 때).

## 성능 측정 (M6)

```bash
dotnet run --project tests/DeskNote.Benchmarks -c Release
```

10,000개 노트를 시드하고 보고서 p13-14의 SLO를 재며, 목표를 놓치면 종료 코드 1을 돌려준다.
Intel i7 / 16GB / SSD, Release 빌드에서 측정한 값:

| 지표 | 목표 | 실측 p95 | |
|---|---|---|---|
| note save (본문 + 체크리스트·태그 투영) | < 30 ms | 1.0 ms | PASS |
| FTS 검색 (한국어) | < 100 ms | 2.5 ms | PASS |
| FTS 검색 (영어) | < 100 ms | 5.3 ms | PASS |
| 시작 시 복원 쿼리 | — | 0.06 ms | PASS |
| 라이브러리 목록 200행 | < 100 ms | 26.4 ms | PASS |
| 라이브러리 검색 | < 100 ms | 2.4 ms | PASS |
| 새 메모 표시 (앱 실측) | < 80 ms | 59.4 ms | PASS |
| idle CPU (메모 8개 열어둔 채 60초) | < 0.5% | 0.026% | PASS |

10,000개 노트의 DB는 14.6 MB, 메모 8개를 띄운 프로세스의 working set은 159 MB.
0.25초마다 메모를 만들고 닫는 버스트에서는 `Window.Activate()` 의 포그라운드 전환 경합으로
p95가 ~540 ms까지 오른다. 사람이 단축키를 누르는 간격에서는 나타나지 않지만 알려진 특성이다.

## 저장과 버전

타이핑이 멈추고 300ms 뒤에 저장이 시작된다(보고서 p5의 250–500ms 대역 중간). 노트마다 타이머가
따로 있어 한 노트에서 치는 동안 다른 노트의 저장이 밀리지 않고, 창이 포커스를 잃거나 닫히거나 앱이
끝날 때는 디바운스를 무시하고 먼저 쓴다.

덮어쓰기 전의 본문은 `note_revisions` 에 남는다. 평범한 타이핑은 5분 간격으로만 체크포인트를
남기고, 본문을 통째로 바꾸는 동작(복구, 일괄 치환, AI 동작)은 항상 먼저 스냅샷을 뜬다.
노트당 50개까지 유지한다.

노트의 `⋯` → **기록** 에서 그 버전들을 본다. 왼쪽에 시각과 출처(`직접 편집` / `AI · 요약`),
오른쪽에 그때의 본문, 아래에 **복원**. 복원 자체도 평범한 본문 쓰기라 지금 본문을 먼저
체크포인트한다 — 잘못 복원하는 것도 되돌릴 수 있다.

AI 적용은 **자동저장 경로를 타지 않는다**. 타이핑은 5분 간격으로 체크포인트하므로, 5분 안에 요약을
두 번 하면 원본이 하나도 남지 않는다 — 보고서 p15 가 요구하는 것의 정반대다. 그래서 AI 적용은
대기 중인 자동저장을 먼저 flush 한 뒤 `BulkReplace` + `RevisionSource.Ai` + 동작 이름으로 직접
쓴다. flush 가 필요한 이유는, 적용 직전에 친 문장이 아직 디바운스 안에 있으면 "AI 이전" 으로
저장되는 버전에 그 문장이 빠지기 때문이다.

## 강제 종료 내성

`tests/DeskNote.Benchmarks` 의 `--crash-writer <db 경로>` 모드로 쓰기 중인 프로세스를 5회 강제 종료한
결과, 매번
`PRAGMA integrity_check = ok`, 외래키 위반 0, 체크리스트·태그 투영 카운트가 노트와 정확히 일치했다.
진행 중이던 쓰기 1건만 본문 없이 남아 트랜잭션 경계가 지켜졌다.

## 데이터 위치

`%LOCALAPPDATA%\DeskNote\notes.db` (WAL). 로그는 `%LOCALAPPDATA%\DeskNote\logs\desknote.log`.

DB를 OneDrive 같은 파일 동기화 폴더에 두지 않는다. WAL 사용 중 파일 단위 외부 동기화는
손상 위험이 있어, 동기화는 이후 note-level operation 복제로 구현한다 (보고서 p6).
