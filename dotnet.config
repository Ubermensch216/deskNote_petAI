# Opt into the Microsoft.Testing.Platform runner. .NET 10 removed VSTest support for it, and
# xunit.v3 hosts its own MTP runner, so `dotnet test` needs this to know which runner to drive.
[dotnet.test.runner]
name = "Microsoft.Testing.Platform"
